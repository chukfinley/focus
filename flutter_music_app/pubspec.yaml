name: flutter_music_app
description: Simple music streaming app with upload
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  just_audio: ^0.9.36
  file_picker: ^6.1.1
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1
  dio: ^5.4.0
  permission_handler: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
